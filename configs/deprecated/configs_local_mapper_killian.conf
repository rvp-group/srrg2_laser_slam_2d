"LaserSLAM2DConfig" { 
  "#id" : 1, 

  // validator used to confirm closurres
  "closure_validator" : { 
    "#pointer" : 2
   }, 

  // reuse existing local map
  "enable_relocalization" : 1, 

  // global solver for loop closures
  "global_solver" : { 
    "#pointer" : 3
   }, 

  // aligner used to register loop closures
  "loop_detector" : { 
    "#pointer" : 1000
   }, 

  // distance between local map origins
  "local_map_distance" : 4, 

  // incremental tracker
  "tracker" : { 
    "#pointer" : 5
   }, 

  // name of the object for being retrieved by the module loader
  "name" : "mapper"
 }

"LaserSLAM2DLoopDetectorConfig" { 
  "#id" : 1000, 

  // min distance between local maps to start aggressive relocalize
  "aggressive_relocalize_graph_distance" : 10, 

  // max distance to attempt global relocalize
  "aggressive_relocalize_graph_max_range" : 20, 

  // factor to pimp the chi2 threshold depending on the lenght
  "aggressive_relocalize_range_increase_per_edge" : 0.07, 

  // aligner used to register loop closures
  "relocalize_aligner" : { 
    "#pointer" : 4
   }, 

  // max chi per inlier, for success
  "relocalize_max_chi_inliers" : 0.02, 

  // min inliers for sucess
  "relocalize_min_inliers" : 300, 

  // min fraction of inliers out of the total correspondences
  "relocalize_min_inliers_ratio" : 0.6, 

  // distance of candidate closures (in local map units)
  "relocalize_range_scale" : 1, 

  // name of the object for being retrieved by the module loader
  "name" : "loop_detector"
 }

"Tracker2DConfig" { 
  "#id" : 5, 

  // al
  "aligner" : { 
    "#pointer" : 6
   }, 

  // aa
  "measurement_adaptor" : { 
    "#pointer" : 7
   }, 

  // al
  "merger" : { 
    "#pointer" : 8
   }, 

  // cc
  "scene_clipper" : { 
    "#pointer" : 9
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"MeasurementAdaptorProjective2DConfig" { 
  "#id" : 7, 

  // projector used to compute the cloud from the scan
  "projector" : { 
    "#pointer" : 10
   }, 

  // range_max [meters]
  "range_max" : 1000, 

  // range_min [meters]
  "range_min" : 0.0, 

  // unproject voxelization resolution
  "voxelize_resolution" : 0.02, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"SceneClipperProjective2DConfig" { 
  "#id" : 9, 

  // projector used to remap the points
  "projector" : { 
    "#pointer" : 11
   }, 

  // resolution used to decimate the points in the scan ona grid [meters]
  "voxelize_resolution" : 0.1, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"FactorGraphClosureValidatorConfig" { 
  "#id" : 2, 

  // ratio to accept a closure after min checks
  "inlier_accept_ratio" : 0.7, 

  // chi2 of an inlier 0.1
  "inlier_chi" : 0.1, 

  // ratio to reject a closure after min checks // 0.3
  "inlier_reject_ratio" : 0.4, 

  // minimum number a closure is checked
  "min_times_checked" : 3, 

  // region around the endpoint of a closure to compute partitions
  "partition_expansion_range" : 8, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"CorrespondenceFinderNN2DConfig" { 
  "#id" : 12, 

  // search algorithm for distance map
  "distance_map_path_search" : { 
    "#pointer" : 13
   }, 

  // max distance for correspondences [meters]
  "max_distance_m" : 3.5, 

  // min cosinus between normals
  "normal_cos" : 0.7, 

  // resolution of the distance map [m/pixel]
  "resolution" : 0.1, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"PathMatrixDistanceSearchConfig" { 
  "#id" : 13, 

  // squared distance [pixel^2] to stop when performing a search
  "max_distance_squared_pxl" : 799.758, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"MergerProjective2DConfig" { 
  "#id" : 8, 

  // max distance for merging the points in the scene and the measurement
  "merge_threshold" : 0.2, 

  // projector to compute correspondences
  "projector" : { 
    "#pointer" : 14
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"ProjectorSpherical2DConfig" { 
  "#id" : 14, 

  // end angle    [rad]
  "angle_max" : 1.5708, 

  // start angle  [rad]
  "angle_min" : -1.5708, 

  // minimum number of points in ball when computing a valid normal
  "normal_min_points" : 5, 

  // range of points considered while computing normal
  "normal_point_distance" : 0.2, 

  // number of laser beams
  "num_ranges" : 180, 

  // max laser range [m]
  "range_max" : 19.9, 

  // min laser range [m]
  "range_min" : 0.32, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"Aligner2DTerminationCriteriaConfig" { 
  "#id" : 15, 

  // chi decay
  "chi_epsilon" : 0.2, 

  // range of correspondences at steady state
  "num_correspondences_range" : 20, 

  // range of num inliers at steady state
  "num_inliers_range" : 20, 

  // range of num outliers at steady state
  "num_outliers_range" : 20, 

  // window used to compute the thresholds
  "window_size" : 5, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"Aligner2DSolverWrapperConfig" { 
  "#id" : 16, 

  // solver instance
  "solver" : { 
    "#pointer" : 17
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"RobustifierCauchyConfig" { 
  "#id" : 18, 

  // threshold of chi after which the kernel is active
  "chi_threshold" : 0.05, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"Aligner2DConfig" { 
  "#id" : 6, 

  // correspondence finder module use in this aligner 19
  "correspondence_finder" : { 
    "#pointer" : 19
   }, 

  // maximum number of iterations
  "max_iterations" : 100, 

  // less than these correspondences at any time will cause the solver to stop on error status
  "min_num_correspondences" : 10, 

  // solver wrapper used in this module
  "solver" : { 
    "#pointer" : 16
   }, 

  // termination criteria, not set=max iterations
  "termination_criteria" : { 
    "#pointer" : 15
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"SolverSE2Plane2PlaneDynamicDiagonalSharedConfig" { 
  "#id" : 17, 

  // pointer to the optmization algorithm (GN/LM or others)
  "algorithm" : { 
    "#pointer" : 20
   }, 

  // maximum iterations if no stopping criteria is set
  "max_iterations" : 1, 

  // the robustifier used for the correspondence factor
  "robustifier" : { 
    "#pointer" : 18
   }, 

  // term criteria ptr, if 0 solver will do max iterations
  "termination_criteria" : { 
    "#pointer" : -1
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"CorrespondenceFinderProjective2DConfig" { 
  "#id" : 19, 

  // min cosinus between normals
  "normal_cos" : 0.99, 

  // max distance between corresponding points
  "point_distance" : 0.3, 

  // projector to compute correspondences
  "projector" : { 
    "#pointer" : 21
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"Aligner2DSolverWrapperConfig" { 
  "#id" : 22, 

  // solver instance
  "solver" : { 
    "#pointer" : 23
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"RobustifierCauchyConfig" { 
  "#id" : 24, 

  // threshold of chi after which the kernel is active
  "chi_threshold" : 0.1, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"SparseBlockLinearSolverCholeskyCSparseConfig" { 
  "#id" : 25, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"MessageFileSourceConfig" { 
  "#id" : 26, 

  // file to read
  "filename" : "", 

  // name of the object for being retrieved by the module loader
  "name" : "source"
 }

"ProjectorSpherical2DConfig" { 
  "#id" : 21, 

  // end angle    [rad]
  "angle_max" : 1.5708, 

  // start angle  [rad]
  "angle_min" : -1.5708, 

  // minimum number of points in ball when computing a valid normal
  "normal_min_points" : 5, 

  // range of points considered while computing normal
  "normal_point_distance" : 0.2, 

  // number of laser beams
  "num_ranges" : 180, 

  // max laser range [m]
  "range_max" : 19.9, 

  // min laser range [m]
  "range_min" : 0.32, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"Aligner2DConfig" { 
  "#id" : 4, 

  // correspondence finder module use in this aligner //12
  "correspondence_finder" : { 
    "#pointer" : 12
   }, 

  // maximum number of iterations
  "max_iterations" : 30, 

  // less than these correspondences at any time will cause the solver to stop on error status
  "min_num_correspondences" : 10, 

  // solver wrapper used in this module
  "solver" : { 
    "#pointer" : 22
   }, 

  // termination criteria, not set=max iterations
  "termination_criteria" : { 
    "#pointer" : -1
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"SolverSE2Plane2PlaneDynamicDiagonalSharedConfig" { 
  "#id" : 23, 

  // pointer to the optmization algorithm (GN/LM or others)
  "algorithm" : { 
    "#pointer" : 27
   }, 

  // maximum iterations if no stopping criteria is set
  "max_iterations" : 1, 

  // the robustifier used for the correspondence factor
  "robustifier" : { 
    "#pointer" : 24
   }, 

  // term criteria ptr, if 0 solver will do max iterations
  "termination_criteria" : { 
    "#pointer" : -1
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"SparseSolverConfig" { 
  "#id" : 3, 

  // pointer to the optmization algorithm (GN/LM or others)
  "algorithm" : { 
    "#pointer" : 28
   }, 

  // if true, solver will bind variables on creation
  "create_on_bind" : 0, 

  // pointer to linear solver used to compute Hx=b
  "linear_solver" : { 
    "#pointer" : 25
   }, 

  // maximum iterations if no stopping criteria is set
  "max_iterations" : 100, 

  // policies used to assign robust kernels
  "robustifier_policies" : [ { 
  "#pointer" : 29
 }, { 
  "#pointer" : 30
 } ], 

  // term criteria ptr, if 0 solver will do max iterations _was_ 31
  "termination_criteria" : { 
    "#pointer" : -1
   }, 

  // name of the object for being retrieved by the module loader
  "name" : "global_optimizer"
 }

"RobustifierPolicyByTypeConfig" { 
  "#id" : 29, 

  // name of the class of the matching factor
  "factor_class_name" : "LoopClosure2D", 

  // robustifier matching the rule
  "robustifier" : { 
    "#pointer" : 32
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"RobustifierCauchyConfig" { 
  "#id" : 32, 

  // threshold of chi after which the kernel is active
  "chi_threshold" : 0.1, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"RobustifierPolicyByTypeConfig" { 
  "#id" : 30, 

  // name of the class of the matching factor
  "factor_class_name" : "SE2PosePoseErrorFactorAD", 

  // robustifier matching the rule
  "robustifier" : { 
    "#pointer" : 33
   }, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"RobustifierCauchyConfig" { 
  "#id" : 33, 

  // threshold of chi after which the kernel is active
  "chi_threshold" : 1, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"SimpleTerminationCriteriaConfig" { 
  "#id" : 31, 

  // ratio of decay of chi2 between iteration
  "epsilon" : 0.001, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"MessageROSBagSourceConfig" { 
  "#id" : 34, 

  // file to read
  "filename" : "mit-killian.bag", 
  "topics" : [ "/FLASER", "/ODOM" ], 

  // verbose
  "verbose" : 0, 

  // name of the object for being retrieved by the module loader
  "name" : "source_ros"
 }

"MessageSortedSourceConfig" { 
  "#id" : 35, 

  // sink from where to request data
  "source" : { 
    "#pointer" : 34
   }, 

  // lag time to sort messages
  "time_interval" : 1, 

  // name of the object for being retrieved by the module loader
  "name" : "sorter"
 }

"PathMatrixDistanceSearchConfig" { 
  "#id" : 36, 

  // squared distance [pixel^2] to stop when performing a search
  "max_distance_squared_pxl" : 1, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"MessageSynchronizedSourceConfig" { 
  "#id" : 37, 

  // name of the frame in the message pack
  "output_frame_id" : "pack_frame_id", 

  // name of the topic in the message pack
  "output_topic" : "pack", 

  // the SORTED source from where we read
  "source" : { 
    "#pointer" : 35
   }, 

  // interval for the messages to be sinchronized
  "time_interval" : 0.05, 
  "topics" : [ "/FLASER", "/ODOM" ], 

  // name of the object for being retrieved by the module loader
  "name" : "sync"
 }

"CorrespondenceFinderNN2DConfig" { 
  "#id" : 38, 

  // search algorithm for distance map
  "distance_map_path_search" : { 
    "#pointer" : 103
   }, 

  // max distance for correspondences [meters]
  "max_distance_m" : 0.2, 

  // min cosinus between normals
  "normal_cos" : 0.99, 

  // resolution of the distance map [m/pixel]
  "resolution" : 0.1, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"PathMatrixDistanceSearchConfig" { 
  "#id" : 103, 

  // squared distance [pixel^2] to stop when performing a search
  "max_distance_squared_pxl" : 10, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }



"CorrespondenceFinderKDTree2DConfig" { 
  "#id" : 39, 

  // max distance for correspondences [meters]
  "max_distance_m" : 0.01, 

  // maximum range for a leaf of the KDTree [meters]
  "max_leaf_range" : 0.01, 

  // minimum number of points in a leaf of the KDTree
  "min_leaf_points" : 20, 

  // min cosinus between normals
  "normal_cos" : 0.8, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"IterationAlgorithmGNConfig" { 
  "#id" : 28, 

  // damping factor, the higher the closer to gradient descend. Default:0
  "damping" : 0.001, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"IterationAlgorithmGNConfig" { 
  "#id" : 27, 

  // damping factor, the higher the closer to gradient descend. Default:0
  "damping" : 0, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"IterationAlgorithmGNConfig" { 
  "#id" : 20, 

  // damping factor, the higher the closer to gradient descend. Default:0
  "damping" : 0, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"ProjectorSpherical2DConfig" { 
  "#id" : 10, 

  // end angle    [rad]
  "angle_max" : 1.55334, 

  // start angle  [rad]
  "angle_min" : -1.5708, 

  // minimum number of points in ball when computing a valid normal 3
  "normal_min_points" : 3, 

  // range of points considered while computing normal 0.2
  "normal_point_distance" : 0.3, 

  // number of laser beams
  "num_ranges" : 180, 

  // max laser range [m]
  "range_max" : 1000, 

  // min laser range [m]
  "range_min" : 0.32, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"ProjectorSpherical2DConfig" { 
  "#id" : 11, 

  // end angle    [rad]
  "angle_max" : 3.1416, 

  // start angle  [rad]
  "angle_min" : -3.1416, 

  // minimum number of points in ball when computing a valid normal
  "normal_min_points" : 5, 

  // range of points considered while computing normal
  "normal_point_distance" : 0.2, 

  // number of laser beams
  "num_ranges" : 360, 

  // max laser range [m]
  "range_max" : 19.9, 

  // min laser range [m]
  "range_min" : 0.32, 

  // name of the object for being retrieved by the module loader
  "name" : ""
 }

"SceneClipperBall2DConfig" { 
  "#id" : 105, 

  // maximum distance of points from origin
  "max_range" : 10, 

  // name of the object for being retrieved by the module loader
  "name" : "scene_clipper"
 }
